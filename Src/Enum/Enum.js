export const ReturnIdOption = Object.freeze({
    RETURN_ID: true,
    NO_RETURN: false
});

/**
 * Encompasses valid amounts that can be fed to the system in the context
 * of finance.
 * 
 * e.g. An Investment returns percentage could be zero initially before it's finalised. 
 */
export const Amount = Object.freeze({
    STARTING_AMOUNT: 0
});